# Dynamic growing array
A program that
- start with dynamic int array of size 5
- read integer values from use until -1
- if array becomes full, allocate a new array with double size, copy old data, free old memory, and cotinue
- print all values after the user stops
- free memory with *delete []*